#include <cstdio>
#include <cstring>

int main()
{
    char s[100];
    scanf("%s", s);
    while ((t = getchar()) != EOF)
    {
        int ok = 1;
        int judge = ;
        for (int i = 1; i < strlen(s); i++)
        {
            if (strchr(s, t) == NULL or strchr(s, t) < judge)
        }
    }
}